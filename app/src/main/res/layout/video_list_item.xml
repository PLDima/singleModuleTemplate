<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/item_background"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/videoIconImageView"
        android:layout_width="wrap_content"
        android:layout_height="200dp"
        android:layout_marginTop="@dimen/item_distance"
        android:maxHeight="200dp"
        tools:layout_height="wrap_content"
        tools:src="@tools:sample/backgrounds/scenic"
        android:contentDescription="@string/videoIconDescription" />

    <TextView
        android:id="@+id/videoNameTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/default_margin"
        android:textAppearance="@style/TextAppearance.AppStyle.YouTubeStyle.Title"
        tools:text="Video Name" />

    <TextView
        android:id="@+id/videoViewCountTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/view_count_top_padding"
        android:textAppearance="@style/TextAppearance.AppStyle.YouTubeStyle.Description"
        app:layout_constraintTop_toBottomOf="@id/videoNameTextView"
        tools:text="view count" />
</LinearLayout>